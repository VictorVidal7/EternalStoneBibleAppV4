# Dependencies & build output
node_modules
coverage
android
ios
.expo
dist
build

# Live agent worktrees (each carries its own copy of touched source files)
.claude

# Lockfiles
package-lock.json
yarn.lock

# Generated / bundled data — not hand-edited source, so not prettier's concern
# (the whole RVR1960 text is embedded as a ~250k-line TS data module, and each
# Bible book ships as a data JSON; formatting them only churns large diffs).
src/data/bible_books
src/lib/database/bible-data-rvr1960.ts
src/lib/database/bible-data-web.ts
src/features/study/data/greekMorphologyCodes.json
src/features/study/data/hebrewMorphologyCodes.json

# Binary asset bundles (images, fonts, the seeded SQLite db, etc.)
assets

# Cloud Functions — separate Node project with its own tooling; not part of
# the main repo's npm run validate (see functions/ setup in the gift-code-
# redemption feature report for how it's gated instead).
functions
